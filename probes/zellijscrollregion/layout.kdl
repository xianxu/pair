layout {
    // The probe's pane process. zellij resolves a relative command against its
    // own cwd, so zellijprobe.WriteLayout rewrites this file's args at run time -- see
    // probes/zellijprobe.
    pane command="sh" {
        args "PROBE_SH"
    }
}
