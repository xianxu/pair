---
type: continuation
slug: launcher-m2
agent: claude
created: 2026-07-02T12:03:58
branch: main
issues: [000099]
---

# Continuation: launcher-m2

## NEXT ACTION

Build **#99 M2 — Runtime seam + create-flow orchestration** on `main` at
`/Users/xianxu/workspace/pair`. #99 is `working`, 1/5 (M1 landed). The design is
approved and durable in `workshop/plans/000099-port-launcher-to-go-plan.md` — read
its "Core architectural move", "Compatibility shim strategy", and the M2 bullet.

Flow: `sdlc state` (confirm #99 working 1/5) → `sdlc change-code --issue 99` was
already run for the whole issue (branch created + judges passed for the plan); for
M2 you do NOT re-run change-code (it's per-issue, already done) — go straight to
implementing M2 on a fresh branch. **Wait** — #99's branch was merged and deleted,
so you're on `main`. Re-run `sdlc change-code --issue 99` to recreate the working
branch (it re-runs the judges; plan-quality was CLEAN, estimate INFO — should pass;
if the estimate-recon gate complains the block is unchanged so it'll pass). Then
implement M2 → `sdlc milestone-close --issue 99 --milestone M2` (SEE THE SDLC BUG
BELOW) → fix findings → `sdlc pr` → `sdlc merge --yes`.

### M2 design (implement this)

Define **`launcher.Runtime`** — the effect seam (M1–M4 `OSRuntime`+`osfs.FS`
pattern; the launcher today has only the narrow `SessionSource`/`HistoricalScanner`
sources, NOT a unified effect seam). Build **`RunLaunch`** for the **create path**
only (native create behind `PAIR_NATIVE_LAUNCH`; the shell launcher stays the
default — nothing user-facing flips until M4). `RunLaunch` must stay a **thin
orchestrator over the pure deciders** (M1's helpers + `DecideLaunch`) — no branching
business logic inline (ARCH-PURE, the change-code plan-quality commitment).

Fold in the change-code INFOs (recorded in #99 Log):
- **Compose `Runtime` from sub-interfaces** (zellij / ui / markers / config / …),
  not one god-interface — so a fake can stub one concern (ISP). The seam is broad
  (~10 effect groups: zellij exec/query, fzf/prompt, markers, cmux, config
  read/write, nvim reap, spawns, tty, env).
- The blocking zellij handoff is an **M3** concern, but note it: the Go launcher
  must **regain control** after zellij exits (to run `cleanup_quit_marker`), so it
  **cannot `syscall.Exec` zellij** — it's fork+wait (`cmd.Run()` with the tty
  passed through). Make that an explicit `Runtime` method contract, not an inline
  discovery, when you reach it.

M2 covers create: name-prompt (zsh `vared` → a Runtime prompt method), the
tag-restart config picker (#000016, fzf → Runtime), config read/write (jq →
`encoding/json` on the seam; M1's `ShouldMigrateLegacyCodex`/`persistedConfigArgs`/
`shouldMintClaudeSessionID` feed it), the claude `--session-id` mint (uuidgen +
collision-stat behind the seam, decision is M1's `shouldMintClaudeSessionID`), env
exports, the `agent-<tag>` write, the two child-spawns (`ensure_title_poller` →
`pair-title.sh`, session-watcher → `pair-session-watch.sh` — wire, don't re-port),
then the `zellij --new-session-with-layout` handoff (fork+wait). Tests: fake-
`Runtime` loop tests for create / name-prompt / tag-restart config picker.

### What M1 already delivered (USE it, in `cmd/internal/launcher`)

- `agentargs.go`: `resumeToken`/`composeResumeArgs` (codex `resume` leads args[0]),
  `codexAltScreenArgs` (idempotent), `shouldMintClaudeSessionID`, `persistedConfigArgs`
  (strips all 3 agents' resume bindings incl. agy `--conversation=` + codex leading
  `resume`), `hasFlag`/`stripValuelessFlag`/`stripFlagAllForms`/`stripCodexResumeSubcommand`.
- `config.go`: `CanonicalConfigPath`/`LegacyCodexConfigPath`, `ShouldMigrateLegacyCodex`,
  `ClaudeTranscriptPath`/`AgyConversationPath`/`CodexSessionsDir`, `encodeCwd`.
- `format.go`: `FormatAge`, `AgeColor`, `TildeAbbrev`, `PaneTitle`, `EmojiTitle`.
- Pre-existing (from #75): `ParseArgs` (decision subset — refuses continue/rename/
  list; extend for the full parse in **M2** when the native entry wires it),
  `DecideLaunch`, `ZellijSource`, `HistorySource`, `Run` (decision-only).

The live launch path (`cmd/pair-go/main.go` → `entrypoint.ResolveLegacyLaunch` →
`syscall.Exec bin/pair-shell`) is the compat shim to keep as the flag-gated
fallback until M4. `bin/pair-shell` section map: see the #99 plan + survey (the
create branch is lines 1725–2287).

## ⚠️ sdlc milestone-close BUG — will bite M2's close (workaround)

`sdlc milestone-close`'s auto boundary-review window picks a **wrong far-back base**
on this ticket → a 6.8 MB diff → `fork/exec claude: argument list too long` →
verdict `not-run` → close aborts. Confirmed twice on M1 (not a PATH-size issue).
**Workaround (from `workshop/lessons.md`):** run the review yourself with the real
base, then finalize with `--no-judge`:

    sdlc judge milestone-review --base "$(git merge-base main HEAD)" --head HEAD --issue 99
    # …fix findings, then:
    sdlc milestone-close --issue 99 --milestone M2 --actual A --verified '…' --no-judge

Put the **real** verdict in the milestone commit's `Review-Verdict:` trailer (the
final `sdlc close` greps commits for it). This is an ariadne/sdlc bug — **worth
filing upstream** (the user should be told); it will recur every milestone.

## State of play

Merged to `main` this session: #93 M4 (PR 56, clipboard helpers → `clipcmd`/
`zellijpane`), then the M5 launcher design, then #99 created + M1 landed (PR 57,
pure launcher helpers). #99 is `working` 1/5. #93 stays `working` 4/5 (closes when
#99 lands — its Done-when includes a Go owner for the launcher). #91 tracker
current.

Sandbox/env gotchas (unchanged): run `make test` / drift-check / pty tests with
`dangerouslyDisableSandbox: true` **and** `env -u PAIR_SESSION_ID -u PAIR_TAG -u
PAIR_DATA_DIR` (session-env leak → false fails). `failed to store: 100001` on
git push is 1Password noise — the ref still updates (check the `->` line).

## Thread arc & user model

Relentless reviewed forward progress through the #91 native-single-binary roadmap:
"continue to #96 and then #93", "continue to M2/M3/M4", then "continue to M5". Each
milestone taken to merged with the full SDLC treatment (durable plan → change-code
judges → implement → milestone-close review → fix → merge → atlas/lessons/tracker).
For M5 the user **explicitly approved** extracting the launcher into its own ticket
(#99) + the phased M1–M5 plan ("makes sense … approve your plan"). They delegate
with terse "continue"s and trust recommendations; they step away (AskUserQuestion
timed out twice — proceed on best judgment, don't block on structure calls already
recommended). Default to acting; honor the gates (`sdlc` verbs, not manual git),
make ARCH-DRY extractions at the moment duplication lands, keep atlas/lessons/
tracker current per milestone, and surface-and-decide on contradictions.

This continuation exists because the session ran long (two full milestone cycles +
the M5 design + the sdlc-bug debugging) and M2 — the largest, riskiest milestone
(new effect seam + orchestration, interaction-heavy lifecycle) — deserves a fresh
context.

## Open questions

Proceed with the recommendations unless genuinely ambiguous.
1. **Tell the user about the sdlc milestone-close window bug** (above) — recommend
   surfacing it + offering to file an ariadne issue. It's not blocking (workaround
   works) but it will recur every milestone and is a real upstream defect.
2. **M2 estimate weight** — the estimate-quality judge noted M3's impl (1.8) is
   light for its complexity vs M4/M5; revisit the M3 line at M3 start with concrete
   scope (not now). M2's line (design=0.9 impl=2.7) is the dominant item, plausible.
