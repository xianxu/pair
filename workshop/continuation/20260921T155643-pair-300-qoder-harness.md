---
type: continuation
slug: pair-300-qoder-harness
agent: qoder
session_id: 64526fef-e056-48ea-bd71-41ef966c6a91
created: 2026-09-21T15:56:43
branch: 000300-integrate-qoder-harness-into-pair
worktree: /Users/xianxu/workspace/pair
issues: [000300]
---

# Continuation: pair-300-qoder-harness

## NEXT ACTION

Start M4 of #300 — **Task 13 Step 1 first**, because the model default must be pinned from the real CLI before any code: run `qoder -p "Reply with exactly: ok"` from `/tmp` and `qoder --list-models`, record both in the issue `## Log`, pick the cheapest fast model as `DefaultQoderModel`. Then Task 13 Steps 2–4 (failing dispatch test → `runQoder` in `cmd/internal/model/model.go` → live `pair-slug` smoke), Task 14 (qoder prompt glyph into `nvim/scrollback.lua` `PROMPT_PATTERN_BY_AGENT`; `distill.go` only if that consumer actually reads qoder sessions — **and Task 14 must name how the Lua table stays in sync with the Go `qoderPromptGlyphs` map**, per the M3 review's note now in the plan Revisions), Task 15 (settings, evidence first: the allowlist surface may not exist — `trustDirectories` + `--permission-mode` may be the whole surface; the `../ariadne` trust addition is an operator decision, ask), then Task 16: `go test ./...` + `sdlc milestone-close --issue 300 --milestone M4 --actual <measured>`.

## State of play

#300 (integrate qoder harness into pair): M1, M2, M3 all closed on this branch; M3 closed 2026-09-21 on a round-10 boundary review **SHIP** (`Review-Window: 301c5381..d2a1e81f`). Remaining: **M4** (Tasks 13–16: slug, glyphs, settings, boundary) and **M5** (Tasks 17–20: standalone smoke, couch round-trip, docs/atlas roster sweep, whole-issue close). `sdlc state` is the index; the issue file carries Spec/Plan/Log; the durable plan carries the Task rows.

Branch `000300-integrate-qoder-harness-into-pair` at `762998e4`, tree clean except four intentionally-untracked files (`.nvimlog`, `.qoder/`, `workshop/plans/000292-carbonyl-browser-tab-close-gate.md`, `…-m1-review.md`) — never commit those.

M3 evidence carries forward: fixtures under `cmd/internal/wrapcmd/testdata/tty/qoder/1.1.60/` (composer.raw, overlay.raw, selection.raw + metadata), the `qoder` row in `harnessTTYProfiles`, and `qoderPromptGlyphs`/`qoderPromptCol` as the single glyph/column authority (M4's consumers derive from it).

## Thread arc & user model

One issue (#300) driven strictly through the SDLC gates; the operator's directives are short ("continue with #300", "M3 milestone close first", "go ahead. compact first") and each turn advances the issue rather than opening new fronts. Two stable traits: the review gates are to be **honored, not dodged** (M3 took ten rounds; every finding was fixed with a mutation-verified pin rather than argued away), and **evidence precedes claims** (live captures before markers, red-first tests, measured active time). Checkpoints are first-class: "compact first" is a standing preference to make state durable before starting the next substantial unit, and the continuation writer (commit + push; restart owned by the writer in a pair/zellij compaction context) is the expected mechanism. Operational decisions the plan marks "operator decision" (e.g. trusting `../ariadne`) are surfaced, not assumed.

## Open questions

On resume, resolve these open questions with the user before continuing with the NEXT ACTION.

- Task 15's `trustDirectories` addition for `../ariadne` is marked an operator decision in the plan — ask before touching config outside the repo.
- No pair/zellij env was present at checkpoint time, so the writer did not restart the session. If the operator expected an actual restart/compaction event, confirm rather than assuming the checkpoint alone was the ask.

## Artifact map

- Issue `workshop/issues/000300-integrate-qoder-harness-into-pair.md` — Spec/Plan/Log; Log's tail: M3 close, the two fix rounds, the side-quest. Read first.
- Plan `workshop/plans/000300-integrate-qoder-harness-into-pair-plan.md` — Tasks 13–16 are M4; final `## Revisions` entry is the M4 Lua-sync note; earlier entries carry M1–M3 deltas and rule corrections.
- Boundary ledgers `workshop/plans/000300-integrate-qoder-harness-into-pair-close-gate.md` (rounds, dispositions, open findings) and `…-m3-review.md` (round-10 SHIP review incl. the advisory this session fixed).
- M3 code: `cmd/internal/wrapcmd/harness_tty.go` (qoder profile row + orientation fields), `composer_recognizers.go` (`qoderPromptGlyphs`, `qoderPromptCol`, `allowHiddenCursor`), `wrap.go` (`detectQoderOverlayOpen`, `overlayVisible`, `firstMarker`, pump scan-before-bound), `orientation.go`; pins in `overlay_test.go`, `picker_overlay_test.go`, `orientation_test.go`.
- Fixtures `cmd/internal/wrapcmd/testdata/tty/qoder/1.1.60/` with `metadata.json` (version, argv, capture time, per-file hashes).
- Atlas touched this session: `architecture.md`, `session-identity.md`, `index.md`, `how-to-bring-up-a-new-harness-cli.md` (marker rules; `resumeform` table).
- Commit chain: `d2a1e81f` (BR-35..BR-40 fixes) → `04ff3b81` (M3 close, SHIP trailer) → `7658299a` (pump side-quest) → `762998e4` (M4 plan note).

## Live deliberations

- Task 14's Lua↔Go glyph sync is undecided by design: checked-in generated table vs a parity test asserting the Lua qoder row matches `qoderPromptGlyphs`. Leaning parity test — no generator infra exists in this repo, and a test reading the .lua source keeps one authority. Decide when writing Task 14.
- Task 14 Step 1 also asks whether `distill.go`'s missing muse row is deliberate; the same check decides qoder's row. Don't fix muse here — log it as a peer finding if it is lagging.

## Decisions & dead ends

- BR-38 was dispositioned as an **atlas exemption** (title-case "Permission Required" may match ordinary prose; exposure bounded by `pickerActive` being consumed by exactly one Enter) rather than requiring co-occurrence with a body marker — the header is what titles every permission picker.
- The BR-35 rule ("scan at full carry+chunk length, bound afterwards") was extended to the shared chunk pump as a side-quest; the earlier "byte-contiguous window cannot be corrupted" claim was true of the bytes and false of the pre-fix scan order — corrected in the plan Revisions.
- Trailer placement: the round-9 FIX-THEN-SHIP trailer was NOT put on the fix commit (`d2a1e81f`) because the gate's review-window base is the last commit carrying a `Review-Verdict:` trailer — a trailer there would have emptied round 10's window. The SHIP trailer went on the close commit (`04ff3b81`).
- Do not re-run `sdlc milestone-close` after a SHIP finalize; fix-and-bundle applies only on FIX-THEN-SHIP.

## Lessons learned

- `sdlc actual --issue 300` under-measures qoder sessions (1.44h vs the measured 3.36h) because ariadne's transcript registry lacks qoder; measure with `sdlc active-time --git-repo . --issue 300 --since <window start> --dir /Users/xianxu/.qoder/projects/-Users-xianxu-workspace-pair --commit-weight 1.0 --threshold-min 15 --include-assistant`.
- `sdlc milestone-close` can exceed a 10-minute foreground tool cap — run it in the background and read its log.
- Mutation-verify every pin (revert the production line, watch exactly the named test go red, restore) before writing any "pinned by" claim; the review gates re-raise unbacked claims (BR-25's family).
- Findings come in families; fix the rule at one authority and sweep every sibling in the same commit, logging deliberate non-extensions with reasons — that is what stopped the M2/M3 rounds from re-raising the same class.
