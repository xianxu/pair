---
type: continuation
slug: m4-build
agent: claude
created: 2026-07-01T22:53:28
supersedes: go-port-m4
branch: main
issues: [000093]
---

# Continuation: m4-build

## NEXT ACTION

Build **#93 M4 — clipboard helpers** on `main` at `/Users/xianxu/workspace/pair`.
The **survey + detailed design are already done and committed** (`91ab3ad`) in
`workshop/plans/000093-port-shell-orchestrators-plan.md` (see the "M4 — clipboard
helpers (detailed)" section). So skip design; resume at the build gate:

`sdlc state` → confirm #93 `working`, 3/5. Then `sdlc change-code --issue 93`
(plan-quality judge; **if estimate-quality re-flags the known M5 optimism
advisory** — it has every milestone — note it in the issue Log and re-run
`sdlc change-code --issue 93 --no-judge`; whole-issue estimate stays 17.4).
Then implement per the committed M4 plan, then `sdlc milestone-close --issue 93
--milestone M4 --actual <from `sdlc actual`> --verified '…'`, fix the
FIX-THEN-SHIP findings (there are always a few, all cheap), `sdlc pr` →
`sdlc merge --yes`.

### M4 design already decided (in the plan) — implement this

- **SHIM pattern, NOT replace-in-place** (differs from M2/M3). The three helpers
  are `*.sh` and `.gitignore` has `!bin/*.sh` (tracks .sh), AND
  `tests/copy-on-select-test.sh` stubs `$PAIR_HOME/bin/clipboard-to-pane.sh` **by
  path**. So: keep `bin/{copy-on-select,clipboard-to-pane,flash-pane}.sh` as
  tracked thin re-exec shims (M1 session-watch.sh style) → 3 gitignored Go
  binaries `cmd/{copy-on-select,clipboard-to-pane,flash-pane}`. The ported
  copy-on-select must **still exec** `$PAIR_HOME/bin/{flash-pane,clipboard-to-pane}.sh`
  (preserving the chain the test drives). Do NOT drop `.gitignore` negations here
  (there are none for these — they match `!bin/*.sh`).
- **Extract `cmd/internal/zellijpane`**: `Parse(jsonBytes) []Pane` (`Pane{ID,
  TerminalCommand, IsFocused, IsPlugin, IsFloating}`, recursive-descent order) —
  the `list-panes --json` walk is needed 3× (opener.firstAgentPaneID +
  copy-on-select focused-pane + clipboard-to-pane nvim-pane). Use it for the 2 clip
  walks now; note opener adoption later.
- **`cmd/internal/clipcmd`** (embeds `osfs.FS`): pure `isNvimCommand(cmd)`
  (`nvim|draft` regex on `terminal_command`, NOT title — the copy-on-select-test
  bug), `quoteFile(dataDir, tag)` (`quote-<tag>`, tag=`PAIR_TAG||PAIR_AGENT||claude`),
  focused/nvim pane selectors, flash defaults (`#50fa7b`/`100`). Seam: clipboard
  copy/paste (pbcopy/pbpaste → wl → xclip), zellij list-panes/set-pane-color/
  focus-pane-id/`write 31` (Ctrl-_), the **detached** flash reset (setsid — survives
  caller exit, like opener's changelog distiller), and `Exec` for the .sh chain.
  Three CLIs: `RunCopyOnSelect(stdin,…)`, `RunClipboardToPane`, `RunFlashPane`.
- **Tests:** Go unit tests (isNvimCommand incl. the parley.nvim-cwd false-positive
  the shell test guards; quoteFile; zellijpane.Parse + selectors) + a fake-Runtime
  loop test per Run. Keep `tests/copy-on-select-test.sh` unchanged (stubs the .sh
  handoff — preserved by the exec chain). **Add `$(BIN_DIR)/copy-on-select.sh` +
  the Go-binary prereqs to `test-copy-on-select`** (M2/M3 both had a missing-prereq
  Important finding — do it upfront).
- **Makefile:** GO_BINS + .PHONY + RUNTIMEBUNDLE_HELPERS + per-binary targets +
  explicit build rules for the 3 Go binaries; the 3 `.sh` shims stay in
  `runtimebundlegen`'s explicitAssetPaths (already there). `make build` regen bundle.
- **Atlas:** update `go-migration-inventory.md` contract rows + Coverage Ledger +
  a `#93 M4` bullet; **sweep call-graph staleness** (`grep -rn '<helper>' atlas/
  README.md`).

After M4: **M5 launcher** (`bin/pair-shell` ~2287 lines) — run `sdlc start-plan`,
elaborate as its own design pass, **very likely its own ticket**. **#93 stays
`open` until the launcher lands** (M3 plan-quality judge's ARCH-PURPOSE note); do
not close #93 after M4. Re-scope the 17.4h estimate if M5 stays one milestone.

## State of play

Merged to `main` this session: #96 (PR 52), #93 M1 (PR 53), M2 (PR 54), M3 (PR 55).
#93 is `working` 3/5. M4 design committed (`91ab3ad`), no M4 code yet.

Reusable shared packages (USE them, don't re-declare): `cmd/internal/procutil`
(Alive/Command), `cmd/internal/osfs` (string-based FS the OSRuntimes **embed**),
`cmd/internal/codexsid` (codex ps/lsof walk). Next extraction to add in M4:
`cmd/internal/zellijpane`.

Conventions proven across M1–M3: reuse already-Go subcommands in-process when
synchronous; keep pure single-source logic in Lua where nvim needs it (invoked via
`nvim --headless`); existing shell process-tests become integration tests against
the Go binaries unchanged; every milestone-close returned FIX-THEN-SHIP (no
Criticals) — fix the cheap findings, commit with the `Review-Verdict:` trailer, add
a Log note + plan `## Revisions`.

Sandbox gotchas: this env blocks `/dev/ptmx`, `ps`, `mktemp` in some dirs — run
`make test` / `runtimebundle-drift-check` / pty tests with
`dangerouslyDisableSandbox: true` **and** `env -u PAIR_SESSION_ID -u PAIR_TAG
-u PAIR_DATA_DIR` (session-env leak → false failures). pty/ps Go tests self-skip
on `os.IsPermission`; run for real under `make test` on a normal machine. The
`failed to store: 100001` on git push/pull is 1Password credential-helper noise —
the ref still updates (check the `->` line).

## Thread arc & user model

Steady milestone-by-milestone march through the #91 native-single-binary roadmap:
"continue to #96 and then #93", then "continue to M2/M3/M4". Each milestone taken
all the way to merged with the full SDLC treatment (durable plan → change-code
judges → implement → auto-dispatched milestone-close review → fix → merge). The
user wants **relentless reviewed forward progress**, honoring the gates (`sdlc`
verbs, not manual git), ARCH-DRY extractions made at the moment duplication would
land (procutil/osfs/codexsid/zellijpane), atlas truth per-milestone, lessons
captured. They surface-and-decide on contradictions (e.g. #96 pair-scribe:
"orphaned→retire" was wrong — the atlas documented it as live `~/.zshrc` tooling;
they chose "route it"). Default to acting; the milestone rhythm is well-worn.

This continuation exists because context hit 80% mid-way with M4 design done but
the build cycle (multi-judge, ~40 min) not started — a fresh session runs it
cleanly. Supersedes `go-port-m4` (which predated the M4 design commit).

## Open questions

Proceed with the recommendations unless genuinely ambiguous.
1. **M5 as its own ticket** — recommend yes (sub-ticket, deps #93; keep #93 open
   until it lands). Confirm before splitting.
