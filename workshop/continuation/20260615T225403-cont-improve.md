---
type: continuation
slug: cont-improve
agent: claude
session_id: 6ce2b8a6-9ee7-438c-98c4-0d21d663c16e
created: 2026-06-15T22:54:03
branch: main
worktree: /Users/xianxu/workspace/pair
issues: [000061, 000105, 000103]
---

# Continuation: cont-improve

## NEXT ACTION
You are reading this in a fresh session **seeded from a continuation produced by the NEW procedure** — that round-trip is itself pair#61 dogfood pass-condition (d). Do these in order:
1. **Resolve the Open questions below with the user FIRST** (per the resume directive) — do not skip ahead.
2. Confirm pass-conditions a–c hold for this very doc: the **Artifact map** points at the real pensive flushed this session (`workshop/pensive/2026-06-15-01-pensive-user-model-throughline.md`); **Open questions** leads with the verbatim resume directive; the new reflective sections (Thread arc & user model, Lessons) are present and non-boilerplate.
3. **Close pair#61.** First check pair's `atlas/` for a park/compaction-flow doc; if one exists, note that `COMPACT_PROMPT` now defers to the continuation datatype (else close with `--no-atlas` + reason — the datatype atlas was already updated in ariadne#105). Then `sdlc close --issue 61 --verified '<dogfood: real Alt+Shift+C drove the new COMPACT_PROMPT → new-shape continuation via the writer; round-trip confirmed by this seeded session>'` (let `--actual` compute), then commit + push the issue file (close before committing — the done-flip must catch the change).

## State of play
- **#61 — pair-continuation improvements** (THIS, pair). status `working`, Plan 3/4 ticked. The COMPACT_PROMPT reroute (`PR #30`) + datatype dep (`ariadne#105`) + runtime-resolution check are done; **the dogfood is the only open item — and this continuation is it.** Close it per NEXT ACTION.
- **ariadne#105 — continuation datatype rewrite** (the substance). **DONE** (PR #42, boundary review SHIP; archived to `../ariadne/workshop/history/`). Flush-first + Thread arc & user model + Open questions + Artifact map + Lessons + NEXT-ACTION-tied-to-arc.
- **ariadne#103 — user-intention AGENTS.md instruction.** `open` stub; **canonical home** for the *live* user-model discipline. Has a back-pointer to #105. Don't chase here unless the user pivots.
- **ariadne#90 — docflow suspend/resume.** `open`; sibling-domain analog. Out of scope.

## Thread arc & user model
**Arc.** Began "let's work on #61, improve the continuation procedure." → mapped the architecture (the procedure lives in ariadne's `continuation.md` datatype; pair only provides mechanics) → gap-analysed the current procedure vs the 5 asks → resolved two forks (repo = ariadne datatype; scope = full) → designed the new skeleton + flush-first → opened **ariadne#105**, rewrote + landed the datatype (SHIP) → answered "what changes in pair?" which surfaced the real pair gap: the Alt+Shift+C `COMPACT_PROMPT` still drove the *old* skeleton → landed **pair#30** → the user hit the real Alt+Shift+C to dogfood (this doc).

**User model** (self-consistent + fits observations). The latent intention under every pivot: **make agents build _and persist_ a model of the user across sessions** (see the flushed pensive). The continuation's new sections are the *persistence* half; ariadne#103 is the *live-maintenance* half; ariadne#90 is the same instinct in docflow. Observed values: dogfoods *live* (literally triggered the keybind to test); routes substance to its correct architectural home (the substrate) even when the issue is filed downstream; trusts `sdlc` discipline and reads its errors as next-action specs; collaborates tersely and decisively ("works / yes / sure") and prizes elegance (the connective-narrative reframe that resolves richness-vs-bloat landed well). The one course-correction — asking "what's the change in pair?" — is what exposed my wrong "no pair change" claim; treat my confident scoping claims as provisional until checked.

## Open questions
On resume, resolve these open questions with the user before continuing with the NEXT ACTION.
- Once **ariadne#103** lands, where is the **canonical** definition of "the user-model + its two criteria"? Today #103 is named canonical and the datatype defers to it, but #103 is a stub, so the criteria are lightly duplicated. Collapse to a single source then?
- Is "model of the user" a **cross-cutting concern** (current: a continuation section + a #103 instruction), or does it deserve its **own durable artifact** that the continuation merely updates?
- For closing #61: does the pair-side change warrant a **pair-atlas** note (the park/compaction flow now defers to the datatype), or is the ariadne#105 atlas update sufficient (→ `--no-atlas`)?

## Artifact map
- **pensive** `workshop/pensive/2026-06-15-01-pensive-user-model-throughline.md` (commit `453261f`) — flushed THIS session; the cross-session user-model throughline. The connective tissue for everything below: #61/#105 (persist) ↔ #103 (maintain-live) ↔ #90 (docflow sibling).
- **pair#61** `workshop/issues/000061-pair-continuation-improvements.md` — the umbrella; its Spec carries the 6 signed-off decisions; Plan 3/4 ticked.
- **ariadne#105** `../ariadne/workshop/history/issues/000105-continuation-datatype-connective-narrative-procedure-flush-to-pensive-thread-arc-user-model-open-questions-lessons.md` (DONE/archived) — read for the full datatype design + rationale.
- **ariadne#103** `../ariadne/workshop/issues/000103-agents-md-instruction-user-intention.md` — canonical home for the live user-model discipline (stub).
- **Key files:** `../ariadne/construct/datatype/continuation.md` — the rewritten datatype, resolved into pair via the `construct/datatype` → `../../ariadne/construct/datatype` **symlink** (so pair gets it for free, no recompose); `nvim/init.lua` `COMPACT_PROMPT` (~2990) — now defers to that datatype, with a comment guarding against re-inlining a skeleton; `bin/pair-continuation` — the writer (enforces only `## NEXT ACTION`; auto-commits+pushes).
- **Repos:** pair = `/Users/xianxu/workspace/pair` (this; `PAIR_DEV=1` → relaunch verb is `pair-dev`). ariadne = `/Users/xianxu/workspace/ariadne` (substrate). Both on `main`.

## Decisions & dead ends
- **Substance → ariadne datatype (#105), not pair-local.** Correct home; pair inherits via the symlink. (decision 6)
- **Flush is `pensive`-only** — `target` needs human review, `meeting-notes` doesn't fit. (decision 3)
- **Writer unchanged** (enforces only `## NEXT ACTION`) — the richer skeleton is authoring discipline, not a binary guard. (decision 4)
- **No `bin/pair` seed-prompt change** — the resume directive is embedded in the generated file instead. (decision 5)
- **Dead end averted:** my first answer "no pair code change" was wrong — `COMPACT_PROMPT` still drove the old skeleton; that drift *was* the real pair#61 deliverable.

## Lessons learned
- A prompt can silently **drift from the datatype/procedure it is meant to invoke**. Guard a prose→procedure coupling with a comment stating *why it defers* (done in `COMPACT_PROMPT`) — cheaper and less brittle than pinning prose in a test.
- The **running nvim caches its config in memory**; editing `nvim/init.lua` doesn't affect a live session until re-source/restart. Matters for any nvim-prompt change and its dogfood.
- **Route a dogfood-found improvement to its architectural home** (the substrate) even when the issue is filed downstream; keep the downstream issue as the umbrella + dogfood. The symlinked `construct/datatype` is why pair got #105 for free.
- **`sdlc` errors are next-action specs:** estimate-before-claim, push-before-merge, close-before-merge (the done-flip rides the PR), atlas-required-on-close, plan-completeness (tick the items a diff actually did). Each error named the fix.
