---
type: continuation
slug: review-m3
agent: claude
created: 2026-06-19T13:38:18
branch: 000066-agentic-review-workbench
worktree: /Users/xianxu/workspace/pair
issues: [000066]
---

# Continuation: review-m3

## NEXT ACTION

Implement the **Alt+r rework** that fixes the M3 real-session smoke bugs, then have the user re-test in a live `pair` session. Four of the five smoke bugs share ONE root cause: the `Alt r` bind spawns a tiny `20%×1` floating "toggle" pane (`Run "pair-review-toggle"`) instead of, like scrollback's `Alt /`, directly spawning a full-screen `100%×100%` pane. Fix = mirror scrollback exactly:
- The bind opens the review pane **full-screen directly** (kills the ~1s delay #1/#3 and the half-size #4). `pair-review-open`'s `tput`-based dims are wrong because `tput` runs inside the 20%×1 toggle pane — use `100%` like scrollback.
- Move the **file-select-vs-toggle branching out of the spawned shell pane into a draft-nvim lua `PairReviewToggle()`**, triggered like `Alt d` (detach): bind = focus draft + `WriteChars ":lua PairReviewToggle()"` + CR. Instant, no intermediate floating pane to confound `hide-floating-panes` (#3) or flap the alive-state (#5). When a review is open → flip visibility from the *tiled* draft (where `are-floating-panes-visible` is reliable); when none → file-select (`:PairReview ` with `complete=file`).

Read the M3 smoke diagnosis in the session/issue first; the exact bin scripts to rewrite are `bin/pair-review-toggle`, `bin/pair-review-open`, the `Alt r` block in `zellij/config.kdl`, and a new `PairReviewToggle()` in `nvim/init.lua` (model on the `Alt d`/`PairConfirmDetach` pattern). This is live-zellij iteration — implement, then the user re-runs the smoke checklist (in the M3 plan, Task 5). Keep it main-thread (a fork can't verify live zellij).

## State of play

pair **#000066** (run `sdlc state` from pair via **ariadne's** sdlc — pair's `./bin/sdlc` is stale):
- **M0–M2: shipped + reviewed (SHIP).** M0 contract (docflow-aligned). M1 record/apply/undo spine. M2 projection/markers/modes. Each passed its auto-dispatched milestone-review (multiple rounds; real bugs caught + fixed each time). Actuals measured: M1 1.87h, M2 +0.7h.
- **M3: headless done, FAILED real-session smoke.** Tasks 1–4 (review pane, Alt+r toggle, id-based poke, marker render) implemented + all headless tests green (`make test-lua` + `make test-review`). NOT milestone-closed (needs the smoke). The user ran the smoke and found **5 bugs**: #1 ~1s Alt+r delay, #2 docflow ENOENT crash on VimEnter (**already fixed** — `docflow.lua` made resilient, uncommitted), #3 review pane auto-hides after ~1s, #4 pane half-size (should be full-screen), #5 Alt+r in review mode wrongly fires `:PairReview`. #1/#3/#4/#5 = the toggle-pane architecture (see NEXT ACTION).
- **M4 (not started):** agent SKILL — recognize "please review"/"ship it"/free-chat (fix-skill-style), memory discovery, run `docflow ship`.
- **ariadne #000120 (filed, not started):** port the fork-for-implementation pattern into the subagent-strategy guidance (AGENTS.md §3 + the two `construct/adapted/superpowers-{dispatching-parallel-agents,subagent-driven-development}` skills).

## Thread arc & user model

Started as a *design brainstorm* on parley.nvim's amnesiac one-shot review; converged on the **eureka**: stop making parley and Claude-Code compete as harnesses — pair's persistent agent is the *conversational* workbench, an embedded nvim is the *document* workbench, a review state file is the seam. Then built it milestone-by-milestone (M0 contract → M1 spine → M2 render → M3 window). The work is a deliberate vertical slice of the **42shots human-AI knowledge-work interface** (a doc tip over a brain/repo corpus, agent discovery, human steering by comment).

User model: a systems-thinker who repeatedly **collapses over-engineering to minimum mechanism** (corrected me toward one-mechanism-many-triggers several times), prizes **repo-centric git-tracked agent-legible state**, **measured-not-typed** actuals, **verify-before-claim**, and **real-session truth** (the M3 smoke surfaced exactly what headless couldn't). He **endorsed fork-for-implementation** ("a stack of context — let's use that more often" → saved to memory) and is hands-on at design forks but delegates bounded implementation. Two corrections this session worth honoring: **use ariadne's sdlc, not pair's stale binary** (active-time-v3 is folded into sdlc's Go, not a python script — I was wrong); and he kept the review's interaction model **fix-skill-shaped** (Alt+Return finishes a human turn + pokes "please review"; ship/chat are conversational in the agent pane; the agent decides turn-ending).

## Open questions

On resume, resolve these open questions with the user before continuing with the NEXT ACTION.

- **docflow resolution:** the review pane has no `docflow` on PATH in a live session (it's ariadne's `scripts/docflow.sh`). Render/undo work without it; round *commits* no-op-with-warning. Should `pair-review-open` set `DOCFLOW_BIN` (locate sibling ariadne?), or is render-only fine for M3 and commits wired in M4?
- **Alt+r rework approach:** is the draft-nvim-lua `PairReviewToggle()` (Alt+d-style) the model the user wants, or a different zellij mechanism? Confirm against his "make it behave exactly like scrollback/changelog."
- **M4 scope** when M3 lands: how much agent-side intelligence (the SKILL) vs. keeping the poke dumb.

## Artifact map

**Read-first order** (issues are NOT auto-loaded): the issue then the three plans then the atlas.
- Issue: `pair/workshop/issues/000066-agentic-review-workbench.md` (Spec + Done-when + the full Log of milestone reviews/actuals).
- Plans: `pair/workshop/plans/000066-agentic-review-workbench-plan.md` (M1 + the M0 contract + `## Revisions` ledger), `…-m2-plan.md`, `…-m3-plan.md` (M3 — has the reviewer's folded-in fixes + the Task 5 manual smoke checklist).
- Atlas: `pair/atlas/review-workbench.md` (current-state map of the module set + the round loop + the git/undofile contract).
- Design rationale (the eureka, in the user's voice): `pair/workshop/pensive/2026-06-18-01-pensive-agentic-review-workbench.md` — **uncommitted** (data artifact; user commits on their schedule).
- Memory: `brain/.../memory/feedback_fork_for_implementation.md` (the fork preference).
- Code (branch `000066-agentic-review-workbench`, worktree `/Users/xianxu/workspace/pair`): `nvim/review/{record,reconstruct,apply,projection,markers,mode,docflow,handoff,init}.lua` (the consumer-half), `nvim/review.lua` (pane init), `nvim/pair_poke.lua` (id-based agent poke), `bin/pair-review-{open,toggle}`, `zellij/config.kdl` (the `Alt r` bind). Tests: `nvim/review/*_test.lua` (`make test-lua`) + `tests/review-*-test.sh` (`make test-review`).
- Peer repo: **ariadne at `/Users/xianxu/workspace/ariadne`** — owns docflow (`scripts/docflow.sh`), the current sdlc (`cmd/sdlc`; build `go build -o /tmp/claude/sdlc-ari ./cmd/sdlc`, run from pair's dir), and issue **#000120**.

## Decisions & dead ends

- **Record = `{old, occurrence, new, explain}`** preserved end-to-end (handoff file == agent commit body); `new_occurrence` (non-overlapping count) added by apply for the resume anchor. Live decorations from the *actual edited ranges*, never re-find.
- **git = checkpoints, not history.** docflow round commits are checkpoints; fine-grained undo is nvim `undofile`; no bespoke sidecar (per-hunk explains ride in the commit body). Settled after reading docflow.
- **Fork-for-implementation** adopted (M2, M3-headless forked successfully; saved to memory).
- **Dead end:** the M3 fork's `pair-review-toggle`-in-a-floating-pane design — slow + confounds floating-pane visibility. Reworking to scrollback's direct full-screen spawn + draft-lua branching (NEXT ACTION).
- Used ariadne's freshly-built sdlc for all closes (pair's binary is stale — missing `actual`, dead python-path explainer).

## Lessons learned

- **Real-session smoke catches what headless can't** — the whole toggle-pane class was invisible to the (green) headless tests; the live smoke found it in minutes. Budget a real-session pass for any zellij/UI milestone.
- **Fresh-eyes reviews earned their keep every milestone** — caught occurrence-mapping mis-anchor (M1), undojoin-targets-wrong-buffer (M1), silent-drop/overlap (M1), the ranged-extmark snapshot fidelity (M2), and the floating-pane-can't-relative-move-focus Critical (M3) — all things I and the first reviewer missed. Always fresh (not fork) for reviews.
- **Match the existing proven pattern exactly** (scrollback) rather than inventing a parallel one — the toggle-pane detour was the cost of deviating.
- **pair's `bin/sdlc` is a stale build** — use ariadne's `cmd/sdlc` directly for any sdlc work on pair until pair's binary is rebuilt.
