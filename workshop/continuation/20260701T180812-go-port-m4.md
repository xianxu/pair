---
type: continuation
slug: go-port-m4
agent: claude
created: 2026-07-01T18:08:12
branch: main
issues: [000093]
---

# Continuation: go-port-m4

## NEXT ACTION

Continue **#93 M4 — clipboard helpers** on `main` at `/Users/xianxu/workspace/pair`.
Run `sdlc state` first; #93 is `working`, 3/5 milestones done (M1/M2/M3 merged).
Port `bin/clipboard-to-pane.sh`, `bin/copy-on-select.sh`, `bin/flash-pane.sh` to
Go, following the **exact per-milestone rhythm proven across M1–M3** (below).

**First, nail the M4 decision the plan left open:** the plan phrases M4 as "port …
**or fold behind the dispatcher**." Decide **port-in-place** (the M2/M3 model:
same-named Go binaries replace the shell scripts, no shim, drop `.gitignore`
negations) vs a `pair <sub>` dispatcher route. Recommendation: **port-in-place**,
consistent with M2/M3 — these are tiny zellij/nvim-invoked-by-name helpers, not
`pair`-internal calls. Confirm callers first: `grep -rn 'clipboard-to-pane\|copy-on-select\|flash-pane' zellij/ nvim/ bin/`. There's a `test-copy-on-select`
Makefile target + `tests/copy-on-select-test.sh` — that's the process-level fake
to keep driving the Go binary.

### The proven M-rhythm (repeat exactly)

1. Survey the shell scripts + callers (zellij config.kdl / nvim) + existing tests.
2. **Detail M4 in** `workshop/plans/000093-port-shell-orchestrators-plan.md`
   (replace the M4 stub, like M2/M3). Cite ARCH-DRY/PURE/PURPOSE.
3. `sdlc start-plan --issue 93` (arch principles), then `sdlc change-code --issue 93`.
   Plan-quality judge runs (~3 min). **If estimate-quality re-flags the known M5
   optimism advisory** (it has every time), note it in the issue Log and re-run
   `sdlc change-code --issue 93 --no-judge` (plan-quality already passed). The
   whole-issue estimate stays 17.4 (unchanged).
4. Implement into a shared `cmd/internal/<pkg>` on the #78 sessionwatch template:
   pure logic + `Runtime` seam **embedding `osfs.FS`** (the M3 extraction — reuse
   it, don't re-declare fs methods) + thin `cmd/pair-*/main.go` shims.
5. Replace the shell scripts at the same PATH names: `git rm` them, drop their
   `.gitignore` negations (the `!bin/…` lines), let `cmd/…` build to those paths.
   **Verify `git ls-files bin/` after** (memory: `bin/` tracking fragility).
6. Tests: Go unit tests for pure logic + a fake-`Runtime` **loop test**
   (first-class per-milestone deliverable, per M1's review) + keep the existing
   shell tests driving the Go binaries unchanged.
7. Makefile: add to `GO_BINS`, `.PHONY`, `RUNTIMEBUNDLE_HELPERS`, a per-binary
   target, and an explicit build rule; add `$(BIN_DIR)/<bin>` prereqs to the
   relevant `test-*` target (M2/M3 both had to fix a missing-prereq Important
   finding — do it upfront). `make build` regenerates the bundle.
8. Atlas: update `atlas/go-migration-inventory.md` contract rows + Coverage
   Ledger + add a `#93 M4` changelog bullet. **Sweep for call-graph staleness**
   before merge (see lessons): `grep -rn '<helper-name>' atlas/ README.md` and
   reconcile any prose describing the old shell mechanism/callers.
9. `sdlc milestone-close --issue 93 --milestone M4 --actual <measured>` (get it
   from `sdlc actual --issue 93` — never type it) `--verified '<behavior>'`. The
   boundary review auto-dispatches (~5–8 min). Fix FIX-THEN-SHIP findings, commit
   with the `Review-Verdict:` trailer, add a Log note + plan `## Revisions`.
10. `sdlc pr` → `sdlc merge --yes` (needs `--yes` — not-done issue). The
    atlas-sync merge judge re-checks; a clean sweep in step 8 makes it pass first try.

After M4: **M5 launcher** (`bin/pair-shell`, ~2287 lines) — run `sdlc start-plan`
and elaborate it as its own design pass, **very likely its own ticket** (per the
`## Estimate` note + the M3 plan-quality judge). **#93 stays `open` until the
launcher actually lands** — do NOT close #93 as done after M4 with the launcher
still in shell (M3 plan-quality judge's explicit ARCH-PURPOSE note). Re-scope the
17.4h estimate if M5 stays one milestone.

## State of play

All merged to `main` this session (#91 = native-single-binary umbrella tracker):

| Issue | Result |
|---|---|
| #96 route pair-wrap + pair-scribe PTY proxies through dispatcher | done, merged PR #52 |
| #93 M1 title poller → `cmd/pair-title` + `cmd/internal/titlepoller` | done, merged PR #53 |
| #93 M2 scrollback/changelog openers → `cmd/internal/opener` | done, merged PR #54 |
| #93 M3 review helpers → `cmd/internal/reviewcmd` | done, merged PR #55 |

Shared packages now in place (reuse them): `cmd/internal/procutil` (Alive/Command,
M1), `cmd/internal/osfs` (string-based FS the OSRuntimes **embed** — opener,
titlepoller, reviewcmd; sessionwatch is byte-based/separate), `cmd/internal/codexsid`
(ps/lsof rollout-uuid walk; canonical home, but `slug`/`sessionwatch` still carry
their own copy — collapsing that triplication is a tracked follow-up).

Established conventions: (a) **replace-in-place, no shim** for zellij/nvim-invoked
helpers (Go binary owns the PATH name; drop `.gitignore` negations). (b) Reuse
already-Go subcommands **in-process** where synchronous (contextcmd, scrollbackcmd),
keep subprocess only where detach is required (changelog distiller via
`SysProcAttr.Setsid`). (c) Keep pure single-source logic in Lua where nvim needs
it (readiness.lua), invoked via `nvim --headless` seam. (d) Existing shell
process-level tests become integration tests against the Go binaries unchanged.

Sandbox notes (this environment blocks `/dev/ptmx`, `ps`, `mktemp` in some dirs):
run `make test` / `runtimebundle-drift-check` / pty tests with
`dangerouslyDisableSandbox: true` **and** `env -u PAIR_SESSION_ID -u PAIR_TAG
-u PAIR_DATA_DIR` (session env leaks cause false failures — memory
`make_test_session_env_leak`). PTY/ps-dependent Go tests self-skip on
`os.IsPermission`; they run for real under `make test` on a normal machine.
The `failed to store: 100001` on `git push`/`git pull` is 1Password
credential-helper noise — the push/pull still succeeds (check the ref line).

## Thread arc & user model

The thread is a steady march through the #91 native-single-binary roadmap: the
user said "continue to #96 and then #93," then drove milestone-by-milestone
("continue to M2", "M3", "M4"). Each milestone got the full SDLC treatment:
design (durable plan) → `change-code` judges → implement → auto-dispatched
milestone-close review → fix findings → merge. Every boundary review returned
FIX-THEN-SHIP (no Criticals); the fixes were always cheap and real (missing test
prereqs, an untested consequential path, atomic-write, a stale doc aside).

User model: the user wants **relentless, reviewed forward progress** — take each
milestone all the way to merged, don't stop half-done, honor the gates rather
than route around them (`sdlc` verbs, not manual git/gh). They value ARCH-DRY
extractions made **at the moment duplication would land** (procutil, osfs,
codexsid), atlas truth kept in sync per-milestone, and lessons captured
(`workshop/lessons.md`) so mistakes don't repeat. They correct scope confidently:
the one time I called something "orphaned → retire" (#96 pair-scribe) on
in-tree-only evidence, the plan-quality judge caught that the atlas documented it
as live `~/.zshrc` tooling — the user chose "route it," and the lesson is to
**surface a contradiction between what you find and how it's described, not
proceed**. Default to acting when the path is clear; the milestone rhythm above
is now well-worn — follow it.

## Open questions

Resolve with the user only if genuinely ambiguous; otherwise proceed with the
recommendations above.

1. **M4 port-in-place vs dispatcher route** — recommend port-in-place (M2/M3
   precedent). Only ask if the clipboard helpers turn out to have a `pair`-internal
   caller that wants a dispatched surface.
2. **M5 as its own ticket** — recommend yes (create a sub-ticket, deps #93, and
   keep #93 open until it lands). Confirm before splitting.
